PathInfo:
    trialPath: "leifrf/StructureTests/"
    fileName: "structureOut"
    executable: "./../../../build/dev/btietz/GATests/AppJSONLearningTests"
    seedDirectory: "/media/leif/Data/Repos/NTRTsim/resources/src/leifrf/3PrismStructureSeed/"
TrialProperties:
    learningType: "structure"
    trialLength: 3000
    generationCount: 1
    generationSize: 40
    scoreMethod: "average"
    fitnessFunction: "distance"
    terrains:
        - "flat"
        - "hill"

nodes:
    bottom1:
        Algorithms:
            MonteCarlo:
                spawnCount: 100
            Elitism:
                selectionMethod: "max"
                survivalCount: 60
            GaussianSampling:
                mutationChance: 1.0
                mutationDev: 0.05
            CrossOver:
    #            selectionMethod: "split" # split or random, future feature
                spawnCount: 10
        Ranges:
            min: -2.5
            max: 0.0
    top3:
        Algorithms:
            MonteCarlo:
                spawnCount: 100
            Elitism:
                selectionMethod: "max"
                survivalCount: 60
            GaussianSampling:
                mutationChance: 1.0
                mutationDev: 0.05
            CrossOver:
    #            selectionMethod: "split" # split or random, future feature
                spawnCount: 10
        Ranges:
            min: 0.0
            max: 10.0

